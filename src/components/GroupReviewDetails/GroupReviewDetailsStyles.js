import { makeStyles } from '@material-ui/core';

export const GroupReviewDetailsStyles = makeStyles(theme => ({
  margin: {
    marginLeft: theme.spacing(1),
    marginTop: theme.spacing(3),
    marginBottom: 0,
  },
}));
